project_folder = "t_project"
source_sql_dialect = "postgres"  # Optional: specify source SQL dialect for conversion (default: "generic")

[environments.dev.connection]
type = "duckdb"
path = "data/t_project.duckdb"

[flags]
materialization_change_behavior = "warn"  # Options: "warn", "error", "ignore"
